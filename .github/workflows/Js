module.exports = {
  presets: [
    ['@babel/preset-env', {
      useBuiltIns: false, // disables polyfill injection
      targets: {
        esmodules: true, // or specify modern environments
      }
    }]
  ]
};
